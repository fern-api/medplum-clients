import { LogLevel, Package, Rule } from "@fern-api/mrlint-commons";
export interface ConsoleMessageMetadata {
    title: string;
    additionalContent: string | readonly string[] | undefined;
    error?: unknown;
    package?: Package;
    rule?: Rule;
}
export declare class ConsoleMessage {
    private level;
    private maxLevel;
    private metadata;
    constructor({ level, maxLevel, ...metadata }: ConsoleMessageMetadata & {
        level: LogLevel;
        maxLevel: LogLevel;
    });
    print(): void;
    private addColorForLevel;
}
