import { Logger, LogLevel, MonorepoLoggers, Package, Rule } from "@fern-api/mrlint-commons";
export declare class ConsoleMonorepoLogger implements MonorepoLoggers {
    private readonly maxLevel;
    constructor(maxLevel: LogLevel);
    getLogger: () => Logger;
    getLoggerForPackage: (p: Package) => Logger;
    getLoggerForRule: (args: {
        rule: Rule;
        package: Package | undefined;
    }) => Logger;
    private getLoggerWithDefaultMetadata;
}
