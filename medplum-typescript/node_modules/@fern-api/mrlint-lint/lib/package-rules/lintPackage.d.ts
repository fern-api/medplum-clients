import { Logger, Monorepo, Package, Result, Rule } from "@fern-api/mrlint-commons";
export declare namespace lintPackage {
    interface Args extends Pick<Rule.PackageRuleRunnerArgs, "packageToLint" | "fileSystems" | "addDevDependency"> {
        monorepo: Monorepo;
        rules: Rule.PackageRule[];
        getLoggerForRule: (args: {
            rule: Rule;
            package: Package | undefined;
        }) => Logger;
    }
}
export declare function lintPackage({ monorepo, packageToLint, rules, fileSystems, getLoggerForRule, addDevDependency, }: lintPackage.Args): Promise<Result>;
