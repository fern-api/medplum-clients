import { Monorepo, MonorepoLoggers, Result, Rule } from "@fern-api/mrlint-commons";
export declare namespace lintMonorepo {
    interface Args {
        monorepo: Monorepo;
        rules: Rule[];
        loggers: MonorepoLoggers;
        shouldFix: boolean;
    }
}
export declare function lintMonorepo({ monorepo, rules, loggers, shouldFix }: lintMonorepo.Args): Promise<Result>;
