import { Logger, MonorepoLoggers, Package, Result } from "@fern-api/mrlint-commons";
export declare type PackageName = string;
export declare type DependencyName = string;
export interface PackageWithRequiredDevDependencies {
    package: Package;
    devDependenciesToAdd: Set<DependencyName>;
}
export declare function addDevDependencies({ devDependencies, shouldFix, loggers, }: {
    devDependencies: Record<PackageName, PackageWithRequiredDevDependencies>;
    shouldFix: boolean;
    loggers: MonorepoLoggers;
}): Promise<Result>;
export declare function addDevDependenciesForPackage({ packageName, existingDevDependencies, devDependenciesToAdd, shouldFix, logger, }: {
    packageName: string;
    existingDevDependencies: readonly DependencyName[];
    devDependenciesToAdd: readonly DependencyName[];
    shouldFix: boolean;
    logger: Logger;
}): Promise<Result>;
