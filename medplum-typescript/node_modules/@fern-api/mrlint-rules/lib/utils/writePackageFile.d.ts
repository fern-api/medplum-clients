import { Logger, Result } from "@fern-api/mrlint-commons";
import { FileSystem } from "@fern-api/mrlint-virtual-file-system";
export declare namespace writePackageFile {
    interface Args {
        fileSystem: FileSystem;
        filename: string;
        contents: string;
        logger: Logger;
    }
}
export declare function writePackageFile({ fileSystem, filename, contents, logger, }: writePackageFile.Args): Promise<Result>;
