export declare type Fetcher = (args: Fetcher.Args) => Promise<Fetcher.Response>;
export declare namespace Fetcher {
    interface Args {
        url: string;
        method: string;
        headers?: Record<string, string>;
        token?: string | (() => string);
        queryParameters?: URLSearchParams;
        body?: unknown;
    }
    interface Response {
        statusCode: number;
        body: unknown;
    }
}
